#file placed in /etc/systemd/system/uwsgi.service
[Unit]
Description=uWSGI Emperor service

[Service]
#ExecStartPre=/usr/sbin/bash -c mkdir -p '/run/uwsgi; chown root:nginx /run/uwsgi'
#ExecStartPre=/usr/sbin/bash -c mkdir -p '/run/uwsgi; chown root:nginx /run/uwsgi
ExecStartPre=chown root:nginx /run/uwsgi
ExecStart=/usr/sbin/uwsgi --emperor /etc/uwsgi.d
Restart=always
KillSignal=SIGQUIT
Type=notify
NotifyAccess=all


[Install]
WantedBy=multi-user.target